---
quick_id: 260613-p7e
slug: fix-sdf-baker-gltf-geometry-loading-to-s
status: planned
date: 2026-06-13
---

# Quick Task: Fix sdf_baker glTF Geometry Loading

## Goal

Fix `sdf_baker` so glTF files with images/material textures load for geometry baking without requiring image decoding.

## Tasks

1. Add a no-op tinygltf image loader for `tools/sdf_baker` that accepts image references but discards pixel payloads.
2. Verify the `sdf_baker` target builds.
3. Run the reported Sponza glTF path through `sdf_baker` with a small bake configuration where possible.
